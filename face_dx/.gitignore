# build artifacts
build/

# models (generated by export_model.py from the ONNX, kept out of git)
models/

# benchmark / debug artifacts
bench*.txt
results_*.jsonl
dump*/
decoded.raw
tmp_*.bin

# editor
*.swp
# regenerable via celeba_pipeline.py --embeddings
reports/*.npz

# regenerable streaming artifacts (resume state + engine-native embeddings)
reports/*.state.jsonl
reports/*.jsonl

# regenerable pipeline staging + streaming artifacts
reports/.tmp_png*/
reports/*.jsonl
reports/*.state.jsonl
reports/*.npz
reports/full_run.log
