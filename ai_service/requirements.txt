# Uni-Activity AI Service v2.4 — Requirements
# Multi-Pipeline: YOLOv8 + NativeSCRFD/native ArcFace (onnx, no insightface pkg) + Liveness

# ── Web Framework ──────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0

# ── Face Recognition ───────────────────────────────────────
# v2.4: the `insightface` pip package is NO LONGER required. Detection
# (det_10g.onnx), 5-point landmarks, norm_crop and the ArcFace fallback
# (w600k_mbf.onnx) run through native_face.py on raw onnxruntime sessions.
onnxruntime>=1.17.0          # CPU only — ถ้ามี NVIDIA ใช้ onnxruntime-gpu
# optional: `onnx` (NOT onnxruntime) lets native_face.py auto-detect mxnet-style
# ArcFace preprocessing (0/1 instead of 127.5). Without it the safe 127.5
# default is used — correct for all glint-style exports incl. w600k_mbf.
scikit-image>=0.19           # SimilarityTransform for norm_crop (was transitive via insightface)

# ── YOLOv8 Face Detector ───────────────────────────────────
ultralytics>=8.2.0            # YOLOv8 (pip install ultralytics)

# ── Image Processing ───────────────────────────────────────
opencv-python>=4.9.0          # หรือ opencv-python-headless บน server
numpy>=1.24.0,<2.0.0

# ── Liveness Detection ─────────────────────────────────────
scipy>=1.12.0                 # FFT analysis
scikit-learn>=1.3.0           # PCA for 512D → 128D reduction

# ── Utilities ──────────────────────────────────────────────
Pillow>=10.0.0
